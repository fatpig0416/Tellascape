import styled from 'styled-components/native';
import { DEVICE_WIDTH, MAP_CONTAINER_HEIGHT } from '../../../utils';

export const MapSnapshotImage = styled.Image`
  width: ${DEVICE_WIDTH};
  height: ${MAP_CONTAINER_HEIGHT};
`;
